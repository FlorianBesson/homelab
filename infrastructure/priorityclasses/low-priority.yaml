apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: low-priority
preemptionPolicy: PreemptLowerPriority
value: 10000
description: "low tier workload, use this for testing, debugging and playground"
